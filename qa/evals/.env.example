# Required for all LLM-as-judge assertions (nli-semantic.yaml, nli-rbac.yaml).
# Not required when running deterministic-only suites (nli-intent.yaml, nli-pii.yaml).
ANTHROPIC_API_KEY=your-anthropic-api-key-here
