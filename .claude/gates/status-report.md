# Status report

The format lives in the `delivery-kit` plugin, at
`${CLAUDE_PLUGIN_ROOT}/gates/status-report.md`.

Read it there. This file is a pointer so the references to it keep resolving; it is not
a second copy of the format.

The human account of the delivery workflow is
[docs/dev/contributing.md](../../docs/dev/contributing.md).
