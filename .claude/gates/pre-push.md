# Pre-push and pre-PR gate

The policy lives in the `delivery-kit` plugin, at
`${CLAUDE_PLUGIN_ROOT}/gates/pre-push.md`. The commands, job names and the targeted
re-run procedure are in [pre-push-mechanics.md](pre-push-mechanics.md).

Read both. This file is a pointer so the references to it keep resolving; it is not a
third copy of the rules.

The human account of this checklist is
[docs/dev/contributing.md](../../docs/dev/contributing.md).
